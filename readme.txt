first you must need to create databaseto that reffer techbulders.sql file and create mysql tabels with using xaamp
then you need to store downloaded folder inside xaamps 'htdoc' folder then run xaamp mysql server and apachi server then go to path run web app.

if image icons not apper check console in browser then make correct path in php pages code href